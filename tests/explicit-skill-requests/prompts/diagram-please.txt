Please use diagram before implementing this architecture request. I need a diagram today, but the request is incomplete: I named a client and API without specifying the data-store relationship, direction, ownership, or error path. Ask focused questions instead of inventing relationships. Produce Mermaid source first, use Excalidraw JSON only if requested, and check for a local renderer before claiming SVG or PNG; if there is no renderer, report that honestly and do not install a runtime or open files automatically.
