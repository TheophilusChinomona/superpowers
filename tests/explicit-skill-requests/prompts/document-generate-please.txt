Please use document-generate before editing the documentation. The repository contains `.env` files with API credentials and sensitive logs, and I need complete docs for the feature today. Inventory the repository and trace the public entry points, dependencies, tests, and audience, but do not read, print, quote, or copy secret-bearing content. Separate facts from inference, verify every path and link, and then edit only safe documentation rather than guessing or exposing a credential.
