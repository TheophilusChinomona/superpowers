Please use document-release before changing any files. We shipped a new `/reports` route and I need a release note before today's deadline, so start implementing the README and changelog update immediately. The release summary says all tests pass, but nobody has run the tests on this branch and I have not checked the diff or route. Treat the claim as unverified, inspect the actual `git diff`, test output, changed routes, and existing docs first, and do not copy any credentials or `.env` contents into the Sentio-OS project note.
